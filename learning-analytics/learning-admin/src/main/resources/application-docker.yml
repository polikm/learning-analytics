# ============================================
# Docker 环境专用配置 - 覆盖 application.yml
# ============================================

spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/learning_analytics
    username: postgres
    password: postgres123

  data:
    redis:
      host: redis
      port: 6379
      password: redis123
